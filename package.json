{
  "name": "agent-learn",
  "version": "1.0.0",
  "private": true,
  "description": "LangChain / Agent 学习项目 monorepo",
  "workspaces": [
    "memory-test",
    "milvus-test",
    "output-parser-test",
    "prompt-template-test",
    "rag-test",
    "tool-test",
    "runnable-test",
    "hello-nest-langchain",
    "cron-job-tool"
  ],
  "scripts": {
    "up": "pnpm update -r",
    "outdated": "pnpm outdated -r",
    "ls": "pnpm ls -r --depth 0",
    "clean": "pnpm exec -- rm -rf node_modules && pnpm install"
  },
  "dependencies": {
    "html-to-text": "10.0.1"
  }
}